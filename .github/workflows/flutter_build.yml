name: Build and Release Windows App

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  create:  # 监听标签创建事件
    types: [created]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Print initial git info
        run: |
          git tag
          echo "Current ref: ${{ github.ref }}"
          echo "Event name: ${{ github.event_name }}"
        shell: bash

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Visual Studio
        uses: microsoft/setup-msbuild@v1.1

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows app
        env:
          CXXFLAGS: "/std:c++20 /await /EHsc"
          CMAKE_CXX_FLAGS: "/std:c++20 /await /EHsc"
        run: |
          flutter config --enable-windows-desktop
          flutter clean
          flutter build windows --release --verbose

      - name: Archive Windows build
        run: |
          cd build\windows\x64\runner\Release
          7z a DigitalScale-Windows.zip *

      - name: Check if build file exists
        run: |
          if (-not (Test-Path "build\windows\x64\runner\Release\DigitalScale-Windows.zip")) {
            Write-Host "Error: Build file not found!"
            exit 1
          }

      - name: Print environment variables
        run: |
          echo "GITHUB_REF_NAME: ${{ github.ref_name }}"
          echo "GITHUB_REF: ${{ github.ref }}"
          echo "GITHUB_EVENT_NAME: ${{ github.event_name }}"
          echo "GITHUB_SHA: ${{ github.sha }}"
          git tag --points-at ${{ github.sha }}

      - name: Publish to GitHub Releases
        if: github.event_name == 'create' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.HYKJ_GITHUB_TOKEN }}
        with:
          files: build/windows/x64/runner/Release/DigitalScale-Windows.zip
          name: "Release ${{ github.ref_name }}"
          tag_name: ${{ github.ref_name }}
          body: "Windows build for ${{ github.ref_name }}"
          draft: true
          prerelease: true

      - name: Print release result (always)
        if: always()
        run: |
          echo "Release step completed."
          echo "Tag: ${{ github.ref_name }}"
          echo "Event: ${{ github.event_name }}"
          echo "Ref: ${{ github.ref }}"
